apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: spring-boot-k8s-gitops-flux-sops-workshop-config
    files:
      - application.yml
